# Phase 1 (dataset preparation + tokenization) dependencies.
# The model-training stack (torch, peft, bitsandbytes, accelerate) is declared
# as the `train` extra in pyproject.toml and pinned here in Phase 2.
pyyaml>=6.0
datasets>=2.16
transformers>=4.38
pytest>=7.4
